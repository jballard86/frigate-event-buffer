# Frigate Event Buffer - Dashboard Card
# =======================================
#
# This iframe card embeds the built-in event viewer from the buffer container.
# The viewer handles camera selection, event navigation, AI summaries, and downloads.
#
# Replace YOUR_BUFFER_IP with the IP/hostname of your Frigate Event Buffer container.
# The port should match your FLASK_PORT setting (default: 5055).

type: iframe
url: "http://YOUR_BUFFER_IP:5055/player"
aspect_ratio: "16:9"
